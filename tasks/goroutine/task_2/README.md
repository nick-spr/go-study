**Что должна делать функция**
* На вход приходит массив чисел nums.
* Нужно конкурентно обработать каждое число с помощью workers горутин.
* Для каждого числа нужно вычислить его квадрат.
* Функция должна вернуть слайс квадратов в том же порядке, что и входные данные.

**Что важно при реализации:**
* Нужно использовать goroutines
* Нужно использовать channels
* Нужно использовать worker pool
* Порядок результатов должен совпадать с порядком входных данных
* Нельзя допускать утечек горутин
* Функция должна корректно работать при:
1. nums == nil
2. len(nums) == 0
3. workers <= 0

**Начальный код**

```go
package main
import "fmt"
func ProcessNumbers(nums []int, workers int) []int {
    // todo
    return nil
}
func main() {
    fmt.Println(ProcessNumbers([]int{2, 4, 6, 8}, 2))
}
```